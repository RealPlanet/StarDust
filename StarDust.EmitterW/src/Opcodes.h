#pragma once
#include "Opcodes.h"

namespace StarDust
{
	namespace Emitter {
		public enum class OpcodeW
		{
			//NoOperation = (int)sde::Opcode::NoOperation,
			//
			//Add = (int)sde::Opcode::Add,
			//Subtract = (int)sde::Opcode::Subtract,
			//Multiply = (int)sde::Opcode::Multiply,
			//Division = (int)sde::Opcode::Division,
			//
			//Call = (int)sde::Opcode::Call,
			//Return = (int)sde::Opcode::Return,
			//
			//LoadConstantI32 = (int)sde::Opcode::LoadConstantI32,
		};
	}
}

